#!/usr/bin/env bash

cd /home/{{ ansible_user }}/asv-collection

{% for item in repositories %}
git submodule update {{ item.project }} || git submodule add -b gh-pages file:///home/{{ ansible_user }}/../{{ item.project }}
{% endfor %}

git add .
git config --global user.email "tom.w.augspurger@gmail.com"
git config --global user.name "ASV Bot"
git commit -am "Autogenerated"
GIT_SSH_COMMAND="ssh -i ~/.ssh/id_bot" git push -f origin
