#!/usr/bin/env bash

cd /home/{{ ansible_user }}/asv-collection

{% for item in repositories %}
echo syncing {{ item.project }}

rsync -a --delete /home/{{ ansible_user }}/{{ item.project }}/{{ item.results }} {{ item.project }}
{% endfor %}

git config --global user.email "pandas.benchmarks@gmail.com"
git config --global user.name "asv-runner"
git add .
git commit -am "Autogenerated"
git push origin master
